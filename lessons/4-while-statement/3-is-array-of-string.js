// Exercise: Check if it's an array of string or not. If empty, it should throw.
// to check if string, you can use this condition : if(typeof myVar === 'string' || myVar instanceof String) {}
//
// Change arguments if necessary
function isArrayOfString(arg) {
  // Write your code here
}

module.exports = isArrayOfString;
